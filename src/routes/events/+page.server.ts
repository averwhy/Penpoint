import type { PageServerLoad } from "./$types";
import type { Semester } from "$lib/models";

export const load: PageServerLoad = async () => {
    let semester: Semester;
    // todo after nick commits
}